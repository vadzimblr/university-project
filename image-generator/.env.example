# Database Configuration (использует существующий PostgreSQL)
POSTGRES_HOST=postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password123
POSTGRES_DB=image_generator_db
POSTGRES_PORT=5432

RABBITMQ_USER=root
RABBITMQ_PASS=root
RABBITMQ_HOST=rabbitmq
RABBITMQ_PORT=5672

# Application Configuration
DEBUG=true
LOG_LEVEL=INFO
APP_PORT=7001

COMFYUI_URL=http://46.16.14.78:8188
MINIO_ENDPOINT=minio:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin123
MINIO_SECURE=false
MINIO_BUCKET=generated-images

# CORS (comma-separated origins)
# CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Optional: public endpoint for presigned URLs (used by FE)
# Use host:port or full URL. Do not include a path prefix.
# MINIO_PUBLIC_ENDPOINT=localhost:9000
# MINIO_PUBLIC_BASE_URL=http://localhost:9000
# MINIO_PUBLIC_SECURE=false
# MINIO_PUBLIC_REGION=us-east-1
